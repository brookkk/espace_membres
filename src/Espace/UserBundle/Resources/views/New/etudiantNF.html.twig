 
  {% extends "EspacePlatformBundle::layout.html.twig" %}

{% block body %}
 

{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}


 <ul class="breadcrumb">
  <li><a href="{{ path('espace_user_homepage') }}">Candidat</a></li>
  <li>MAJ</li>
 
<h3>CV</h3>




{{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.experiences) }}




    <div class="tags" data-prototype="{{ form_widget(form.experiences.vars.prototype)|e('html_attr') }}">
        {# iterate over each existing tag and render its only field: name #}
        {% for exp in form.experiences %}
            <div class="tag">{{ form_row(form.mobilite) }}</div>
        {% endfor %}
    </div>

{{ form_end(form) }}









{#
<div >
  {{ form(form) }}
</div>
#}
 {% endblock %} 